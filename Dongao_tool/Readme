asm2cgen.py:
	this script depends on c_gen in the same dir.
	
	command line example:
	python asm2cgen.py output_dir result_path

	output_dir should recursively contain *.echo,*.AsmFunName,*.lst file
	result_dir_path is the path of generated c file

	c_gen_py.sh is a concrete example to run this tool.

interface_gen.py:
	this script depends on interface_gen in the same dir.
	
	command line example:
	python asm2cgen.py module_dir header_path  info_path  callgraph_path
	
	module_dir: a specific EDK2 module,which should directly contain DEBUG and OUTPUT dir. 
	header_path:path to generate header file
	info_path:path to generate info file
	callgraph_path:the call graph file should be generated by DotCallGraph.so
	
	Don't change arguments order.

Please also refer to https://bitbucket.org/uefi/llvm-clang-samples/src/b65395ee41b56309c9241d43ef35366d9be673af/readme.txt
